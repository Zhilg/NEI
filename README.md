# Автономный пакетный конвейер обработки PDF

Полностью автономный конвейер для пакетной обработки документов. Система рекурсивно обходит разрешённые каталоги, преобразует PDF-документы в Markdown, извлекает сущности и публикует результаты в MinIO.

Текстовый слой PDF намеренно не используется: каждая страница обрабатывается исключительно как изображение.

Перед передачей в MinerU каждый PDF преобразуется в растровые RGB-изображения страниц с фиксированным разрешением (DPI) и сохранением координат преобразования. Затем каждая страница проходит через локальный модуль масштабирования SwinIR x4. Если после обработки изображение становится меньше исходного, превышает допустимый предел по числу пикселей, теряет детализацию либо заметно обрезает диапазон яркости, улучшенная версия сохраняется только как артефакт аудита. Для дальнейшей обработки в MinerU используется исходное изображение, а причина отказа от улучшенной версии фиксируется в журнале. Ни модуль рендеринга, ни подсистема контроля качества не обращаются к текстовому слою PDF и не используют OCR.

MinerU получает только отобранные изображения страниц и формирует локальный файл `middle.json`. Этот файл сохраняется без изменений как исходный артефакт аудита, однако содержащиеся в нём OCR-данные, текст, Markdown, HTML и LaTeX никогда не используются как источник содержимого документа. Внутренний `layout_manifest` хранит геометрию каждого блока, его тип, порядок чтения, иерархию, взаимосвязи и область обрезки, включая блоки новых типов, которые могут появиться в будущем. Таблицы, изображения, диаграммы, формулы, колонтитулы, печати и подписи не удаляются. Их содержимое будет восстановлено на отдельном этапе с помощью Qwen-VL.

PaddleOCR не формирует собственную структуру страницы. Детектор `PP-OCRv5_server_det` выделяет строки только внутри текстовых блоков, определённых MinerU. Маршрутизатор запускает East-Slavic PP-OCRv5 для документов на русском, украинском, белорусском и английском языках, Cyrillic PP-OCRv5 — для остальных языков с кириллической письменностью, а PP-OCRv6 Medium — для поддерживаемых письменностей на основе латиницы и CJK. Для каждого распознанного токена сохраняются координаты на странице (`bbox`), идентификатор блока, уровень уверенности детектора и распознавателя, тип письменности, язык, идентификатор модели, её ревизия и изображение строки. Неподдерживаемая письменность не преобразуется в текст: она сохраняется как графическое свидетельство с пометкой `unsupported_script`, которая затем передаётся Qwen-VL.

Qwen2.5-VL-32B выполняет реконструкцию документа в рамках единого прохода на выделенном слоте роли GPU0. Модель получает выбранные изображения страниц, все относящиеся к ним фрагменты блоков, полное описание структуры документа и порядка чтения из MinerU, результаты PaddleOCR и сведения о распознавании текста. Если страница слишком велика, контроллер разбивает её только с учётом ограничений на объём изображений и количество блоков, после чего детерминированно объединяет результаты в единый Markdown-документ без дополнительного этапа проверки. Структурированный вывод обязан вернуть каждый `block_id` ровно один раз и строго в исходном порядке чтения. Исправление OCR допускается только для уже существующего токена в пределах того же блока и только при наличии визуального подтверждения. В том же ответе модель расшифровывает таблицы, изображения, диаграммы, графики, формулы, печати и подписи, а также формирует отчёт о расхождениях OCR, нечитаемых областях, отсутствующих блоках и очевидных ошибках в суммах, датах, нумерации и ссылках.

## Логика работы

![Схема автономного конвейера пакетной обработки PDF](docs/pipeline-overview.svg)

## Компоненты

| Компонент         | Что это                                                                                                                                | Назначение                                                                                                                                                                                                 | Ресурс              |
| ----------------- | --------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- |
| PostgreSQL        | База данных, которая помнит, что сейчас происходит с каждым документом.                                                                | Основное хранилище состояния системы: очередь заданий, аренда задач (leases), повторные попытки, статусы и резервирование ресурсов                                                                         | CPU / диск          |
| MinIO             | Локальное файловое хранилище для больших файлов, похожее на S3.                                                                        | Объектное хранилище промежуточных артефактов и итоговых неизменяемых результатов обработки; обеспечивает обмен данными между этапами конвейера и долговременное хранение выходных файлов                   | Локальное хранилище |
| Render            | Программа, которая превращает каждую страницу PDF в обычную картинку.                                                                  | Детерминированное преобразование PDF в растровые изображения страниц; текстовый слой и встроенная разметка игнорируются для обеспечения одинакового входа на всех последующих этапах                       | CPU                 |
| SwinIR x4         | Нейросеть для аккуратного увеличения и повышения чёткости изображения страницы.                                                       | Повышение разрешения страниц для улучшения качества последующего анализа и OCR; автоматически возвращается к исходному изображению, если масштабирование не улучшает результат                             | GPU1                |
| MinerU 3.4        | Инструмент, который смотрит на страницу как на макет и отмечает: здесь заголовок, тут таблица, картинка или формула.                   | Анализ макета документа: выделение структурных элементов (заголовков, абзацев, таблиц, рисунков, формул), определение порядка чтения и построение иерархической структуры документа                        | GPU1                |
| PaddleOCR         | Инструмент для чтения текста с картинки, как очень точный сканер текста.                                                               | Построчное распознавание текста исключительно внутри текстовых областей, выделенных MinerU; преобразование растрового текста в машинно-читаемое представление                                              | GPU1                |
| Qwen2.5-VL-32B    | Большая мультимодальная нейросеть: одновременно понимает картинку страницы и текст, найденный OCR.                                    | Верификация и исправление результатов OCR, интерпретация нетекстового содержимого (таблиц, изображений, диаграмм, формул) и объединение всех элементов документа в единый Markdown с сохранением структуры | GPU0                |
| Fenic + Qwen3-14B | Схема извлечения данных и языковая модель, которые превращают готовый текст в понятные поля: люди, организации, даты и суммы.          | Извлечение именованных сущностей и структурированных данных из итогового Markdown по заданной схеме; формирование нормализованного структурированного представления документа                              | GPU0                |


## Что сохраняет MinerU

Внутренний `layout_manifest` сохраняет полное описание каждого блока документа без потери информации: текст, заголовки, списки, таблицы, изображения, диаграммы, формулы, колонтитулы, сноски, печати и подписи. Для каждого блока фиксируются его идентификатор (`block_id`), номер страницы, координаты (`bbox`), положение в иерархии, связи с другими блоками, порядок чтения и ссылка на соответствующий фрагмент изображения.

OCR применяется только к текстовым блокам. Все остальные элементы передаются в Qwen-VL вместе с их изображениями и служебными метаданными. Модель распознаёт содержимое таблиц, интерпретирует диаграммы, графики и формулы, после чего включает полученную информацию в итоговый Markdown-документ.

## Запуск

Для локальной машины и сервера используется один Compose-файл: `infra/compose/local.yml`. Отдельной target-конфигурации, systemd-юнитов, release bundle, подписей и ключей нет.

Код, модели, команды MinerU/PaddleOCR, входящие PDF и постоянные данные не копируются в Compose-контейнеры. Они монтируются с хоста. Windows-скрипт собирает `local/idp-app`, а AWQ-веса Qwen скачивает в `transfer/models`. На Linux images импортируются, а модели монтируются read-only как `/models`.

Подготовьте на хосте следующие каталоги:

```text
transfer/
  models/
    qwen-vl/             # Qwen2.5-VL-32B-Instruct-AWQ
    qwen3/               # Qwen3-14B-AWQ
data/
  input/                 # PDF для обработки
  tools/
    mineru/run           # исполняемая локальная команда MinerU
    ocr/                  # detector, route и три recognizer-команды
  runtime/               # PostgreSQL, MinIO и staging
```

Команды MinerU и OCR получают только пути к временным файлам внутри worker. Они должны поддерживать placeholders, уже передаваемые Compose: `{images}`, `{output}` для MinerU и `{input}`, `{output}` для OCR.

Укажите доступные локальные образы моделей и поднимите стек:

```bash
# Лёгкий режим: PostgreSQL, MinIO, миграции, controller и operator.
export IDP_PIPELINE_PROFILE_VERSION=2026-07-20.1
docker compose -f infra/compose/local.yml up -d

# Полный режим: дополнительно Qwen-VL, Qwen3 и worker с GPU.
docker compose -f infra/compose/local.yml --profile models up -d
```

Для сервера используйте ту же команду; пути к mounts при необходимости задаются переменными `IDP_SOURCE_ROOT`, `IDP_INPUT_ROOT`, `IDP_DATA_ROOT`, `IDP_MODELS_ROOT` и `IDP_TOOLS_ROOT`.

`IDP_PIPELINE_PROFILE_VERSION` нужно менять при любом изменении кода, моделей, инструментов или параметров, влияющих на результат. Так совпадающий PDF не получит результат, созданный прежней конфигурацией.

Контур управления на базе PostgreSQL использует механизм `FOR UPDATE SKIP LOCKED`, поддерживает аренду заданий с восстановлением по heartbeat, повторные попытки выполнения, помещение заданий в карантин, распределение ресурсов, контракт хранения артефактов в MinIO и атомарную публикацию указателя на итоговый результат.

Batch API принимает только абсолютные пути, расположенные внутри каталогов, перечисленных в `IDP_ALLOWED_ROOTS`. Символические ссылки игнорируются. Для каждого файла сохраняется информация о принятом решении, а перед постановкой задания в очередь PDF копируется из проверенного файлового дескриптора во временное хранилище артефактов. Это гарантирует, что дальнейшая обработка всегда выполняется по неизменяемой копии исходного документа.

Команда отправки создаёт неизменяемый снимок входных данных и сразу возвращает идентификатор пакета. Все operator-команды запускаются в одноразовом Compose-контейнере, поэтому Python не требуется устанавливать на хосте:

```bash
docker compose -f infra/compose/local.yml run --rm operator \
  idp batch submit /input --profile default
```

Проверить текущее состояние обработки и получить полный отчёт можно следующими командами:

```bash
docker compose -f infra/compose/local.yml run --rm operator \
  idp batch status <batch-id>
docker compose -f infra/compose/local.yml run --rm operator \
  idp batch report <batch-id> --format json
```

Контроллер продолжает обработку независимо от состояния пользовательского терминала. Если контроллер или исполнитель завершается аварийно, задание автоматически возвращается в очередь после истечения срока аренды (`lease`).

Отчёт всегда содержит сведения обо всех документах независимо от результата обработки: поставленных в очередь (`queued`), повторно использованных (`reused`), пропущенных (`skipped`), помещённых в карантин (`quarantined`) и отменённых (`cancelled`).

Повторная обработка возможна только для элементов со статусом `quarantined`. При этом используется уже сохранённая неизменяемая копия исходного PDF, а не файл, который мог измениться после постановки задания в очередь.

```bash
docker compose -f infra/compose/local.yml run --rm operator idp batch report <batch-id> --format csv
docker compose -f infra/compose/local.yml run --rm operator idp batch cancel <batch-id>
docker compose -f infra/compose/local.yml run --rm operator idp batch retry <item-id>
```

Если содержимое PDF полностью совпадает (по SHA-256), а значение `pipeline_profile_hash` остаётся неизменным, система повторно использует уже опубликованный итоговый пакет результатов. Промежуточные, отменённые и незавершённые результаты никогда не переиспользуются.

## Результаты

Для каждого PDF, успешно прошедшего техническую обработку, в MinIO публикуется неизменяемый пакет результатов:

```text
final.md
entities.json
manifest.json
```

Файл `manifest.json` содержит контрольные суммы, информацию о происхождении всех артефактов, версии профиля, моделей, промптов и схем, сведения о покрытии OCR, выводах VLM, решениях о возврате к исходным данным, количестве попыток обработки и итоговую оценку качества (`quality=pass|warning|failed`).

Статус `quality=failed` не означает, что результат отбрасывается. Если документ удалось обработать технически, пакет всё равно публикуется со статусом `COMPLETED_WITH_WARNINGS`.

Документы, которые невозможно обработать по техническим причинам, получают статус `QUARANTINED` и не блокируют обработку остальных файлов в пакете.


## Подготовка на Windows 11

Скрипт собирает application image, запускает unit-тесты, проверяет/скачивает модели и сохраняет Docker images в tar. PostgreSQL, MinIO и Compose на Windows он не запускает. На Windows интернет используется для `docker pull`, Python wheels и первой загрузки `Qwen/Qwen2.5-VL-32B-Instruct-AWQ` и `Qwen/Qwen3-14B-AWQ`. Если repository marker, config и все shards уже присутствуют, повторной загрузки нет; незавершённая загрузка возобновляется. Для model files создаётся `transfer/models/SHA256SUMS`.

```powershell
$env:HF_TOKEN = (New-Object System.Net.NetworkCredential("", (Read-Host "HF token" -AsSecureString))).Password
.\scripts\export-images-windows.ps1
Remove-Item Env:HF_TOKEN
```

`HF_TOKEN` необязателен для публичных snapshots, но полезен при ограничениях Hugging Face и rate limits. Используйте token с правом `read`. Значение существует только в текущей PowerShell-сессии, не записывается в `.env`, metadata, Docker image или git и очищается последней командой.

Успех обозначается только строкой `=== IDP EXPORT COMPLETE ===` и файлом `transfer/EXPORT-COMPLETE.txt`. Они появляются после unit-тестов, загрузки моделей, `docker save`, checksum и metadata. Если marker отсутствует, экспорт не завершён. На Linux копируется весь репозиторий вместе с `transfer/` и `data/tools/`.

## Запуск на Linux

Сделайте Linux-скрипт исполняемым и просто запустите его из репозитория:

```bash
chmod +x scripts/import-images-linux.sh
./scripts/import-images-linux.sh
```

Скрипт проверяет archive SHA-256, metadata, completion marker и SHA-256 всех model files, затем выполняет `docker load`. `transfer/models` монтируется в Qwen containers как `/models:ro`. После запуска выполняются healthcheck и проверка блокировки внешнего HTTPS. Compose использует `pull_policy: never` и внутреннюю Docker network.

## Как подать PDF на обработку

Скопируйте один или несколько PDF в каталог `data/input` **до запуска Linux-скрипта**. Тогда скрипт после старта автоматически отправит все найденные PDF в batch:

```bash
cp ~/Downloads/document.pdf data/input/
./scripts/import-images-linux.sh
```

Если PDF были добавлены уже после запуска, отправьте их вручную:

```bash
cp ~/Downloads/document.pdf /opt/idp/repo/data/input/
cd /opt/idp/repo
docker compose -f infra/compose/local.yml run --rm operator \
  idp batch submit /input --profile default
```

Команда напечатает `batch_id`. По нему смотрят прогресс и итоговый отчёт:

```bash
docker compose -f infra/compose/local.yml run --rm operator \
  idp batch status <batch_id>
docker compose -f infra/compose/local.yml run --rm operator \
  idp batch report <batch_id> --format json
```

Финальные файлы лежат в MinIO bucket `idp-artifacts` по пути `results/<item_id>/<source_sha256>/`: `final.md`, `entities.json`, `manifest.json` и `reconstruction_manifest.json`. MinIO Console доступна по адресу `http://localhost:9001`; стандартный логин и пароль: `minioadmin` / `minioadmin`. Физические данные MinIO сохраняются в `data/runtime/minio`, но читать этот каталог вручную не нужно.

## Проверка и обновление

Быстрая проверка базы и MinIO после запуска:

```bash
docker compose -f infra/compose/local.yml run --rm operator
```

Проверка вместе с локальными model services:

```bash
docker compose -f infra/compose/local.yml run --rm operator idp healthcheck
```

Для тестов без GPU и моделей используется тот же application image:

```bash
docker compose -f infra/compose/local.yml run --rm operator pytest
```

Чтобы применить изменение Python-кода, измените `IDP_PIPELINE_PROFILE_VERSION` и перезапустите нужный сервис. Если изменились зависимости в `pyproject.toml`, снова запустите Windows-скрипт: он пересоберёт application image, проверит тесты и создаст новый archive.

```bash
docker compose -f infra/compose/local.yml --profile models up -d --force-recreate controller worker
```

Для остановки с сохранением PostgreSQL, MinIO и staging достаточно `docker compose -f infra/compose/local.yml down`. Удаление каталога `data/runtime` удаляет всё постоянное состояние.

## Тестирование

Стратегия тестирования разделена на два уровня.

- Локальный CI запускает модульные тесты, проверки схем, очередей, хранилища и интеграционные тесты с имитацией внешних компонентов. Для этого не требуются GPU и веса моделей.
- На целевом сервере перед повышением версии моделей, среды выполнения или профиля запускаются тесты с использованием реальных моделей: возобновляемые smoke-тесты, а также canary- и soak-проверки.

Пока для документов отсутствует полноценный размеченный эталонный набор данных (ground truth), система не публикует количественные показатели точности. Вместо этого проверки качества подтверждают корректность схем данных, происхождение артефактов, сохранность подтверждающих данных, механизмов восстановления после сбоев и соблюдение ограничений по используемым ресурсам.
